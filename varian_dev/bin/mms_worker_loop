#!/usr/bin/env bash
set -euo pipefail

cd ${M3D_APP_DIR}
activate_m3d

while true
do
    mms_worker_parent --testing
    echo "#############################################"
    echo "Ran mms_worker_parent --testing, sleeping now"
    echo "#############################################"
    sleep 5
done
