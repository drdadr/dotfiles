[diff]
    tool = bc3
[difftool]
    prompt = false
[difftool "bc3"]
    cmd = BComp.exe "$LOCAL" "$REMOTE"
[merge]
    tool = bc3
[mergetool]
    prompt = false
[mergetool "bc3"]
    trustExitCode = true
    cmd = BComp.exe "$LOCAL" "$REMOTE" "$BASE" "$MERGED"
